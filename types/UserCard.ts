type UserCardProps = {
  variant?: "default" | "menu"; // "default" is the variant used in the `Header` component and "menu" is the variant used in the `Menu` component
};
export default UserCardProps;
